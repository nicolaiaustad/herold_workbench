{
  "name": "architecture-viz",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate-data && tsc && vite build",
    "generate-data": "tsx src/parser.ts . ./src/graph-data.json",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@types/three": "^0.160.0",
    "three": "^0.160.0"
  }
}
